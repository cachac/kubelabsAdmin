apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-helm
spec:
  rules:
  - host: carlos63.kubelabs.tk
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: svc-helm-app
            port:
              number: 80

